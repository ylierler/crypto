\documentclass{article}


\usepackage[margin=1in]{geometry} % full-width

% AMS Packages
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{amssymb}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{xspace}
\usepackage{todo}

\newcommand{\ar}{\leftarrow}

\newcommand{\sub}[3]{#1_{#2}^{#3}}

\title{Queen C and Cryptarithmetic\\ \normalsize{Incomplete Draft}}

\begin{document}
	\maketitle
\section{N-Queens Program C}
The lines presented below change Program B in {\em Reasoning about Logic Programming Solutions
to the N-Queens Puzzle} by Vladimir Lifschitz [QVL]
to Program C. In particular Program C is obtained by modifying Program B
as follows 
\begin{itemize}
\item replace the last two rules of Program B with the content of the  presneted lines.
\end{itemize}
    \lstinputlisting[language=Prolog, numbers=left, caption="Part of Program C"]
{queen4.lp}

We now transcribe these lines in a ``cannonical manner'':

\begin{align}
&d1(X,Y,X-Y+n) \ar X = 1..n\wedge  Y = 1..n.\label{r:d1}\\
&d2(X,Y,X+Y-1) \ar X = 1..n\wedge Y = 1..n.\label{r:d2}\\
&\ar Z = 1..n\times 2-1\wedge not\ count \{ X,Y: q(X,Y) , d1(X,Y,Z) \} \leq 1.\label{r:countd1}\\
&\ar Z = 1..n\times 2-1\wedge not\ count   \{X,Y:  q(X,Y) , d2(X,Y,Z) \} \leq 1.\label{r:countd2}    
\end{align}

\section{Program C as a forumla}
We now transform these rules into formulas following the approach of [QVL]. To do so, we start by rewriting these rules in a numeric normal form.

\begin{align}
&d1(X,Y,X-Y+U) \ar X = 1..U\wedge  Y = 1..U\wedge U=n.\label{nr:d1}\\
&d2(X,Y,X+Y-1) \ar X = 1..U\wedge Y = 1..U\wedge U=n.\label{nr:d2}\\
&\ar Z = 1..U\times 2-1\wedge not\ count \{ X,Y: q(X,Y) , d1(X,Y,Z) \} \leq 1\wedge U=n.\label{nr:countd1}\\
&\ar Z = 1..U\times 2-1\wedge not\ count   \{X,Y:  q(X,Y) , d2(X,Y,Z) \} \leq 1\wedge U=n.\label{nr:countd2}    
\end{align}

In the sequel, we use letters $I,J,K,N$ and their variants $I_1,J_1,\ldots$ to denote integer variables. Letters $X,Y,Z,V,U$ and their variants $X_1,Y_1,\ldots$ are used to denote general variables.  


Rules~\eqref{nr:d1} and ~\eqref{nr:d2} are captured by the following formulas:
\begin{align}
&\forall I J  K ((1\leq I\leq K\wedge 1\leq J\leq K \wedge K=n) 
\rightarrow  d1(I,J,I-J+K) ).\label{f:d1}\\
&\forall I J K ((1\leq I\leq K\wedge 1\leq J\leq K \wedge K=n)\rightarrow d2(I,J,I+J-1)).\label{f:d2}
\end{align}

The representations of rules~\eqref{nr:countd1} and~\eqref{nr:countd2} include the constants $s_6$ and $s_7$, which are defined as follows:
\begin{align}
&\forall K V(V\in s_6(K) \leftrightarrow \exists I J( V=(I,J)\wedge q(I,J)\wedge d1(I,J,K))).\label{f:s6}\\
&\forall K V(V\in s_7(K) \leftrightarrow \exists I J( V=(I,J)\wedge q(I,J)\wedge d2(I,J,K))).\label{f:s7}
\end{align}
Now,  
rules~\eqref{nr:countd1} and~\eqref{nr:countd2}  
are captured by the following formulas:
\begin{align}
        &\forall K N\neg(   1\leq K\leq N\times 2-1\wedge \neg count(s_6(K))\leq 1\wedge N=n).\label{f:countd1}\\
        &\forall K N\neg(   1\leq K\leq N\times 2-1\wedge \neg count(s_7(K))\leq 1\wedge N=n).\label{f:countd2}
\end{align}


To sum up,  rules of Program C are represented by formulas  (L11), (L18), (L20), where by letter L we annotate numbers coming from [QVL] and~\eqref{f:d1},~\eqref{f:d2},~\eqref{f:countd1},~\eqref{f:countd2}.  

\section{Completion of Program C}
We are now concerned with building completion for Program C.

Sentences~\eqref{f:d1} and~\eqref{f:d2} are not completable. To make them completable we rewrite them using intuitionisticly equivalent transformations as follows  
\begin{align}
&\forall I J K 
X Y Z((1\leq I\leq K\wedge 1\leq J\leq K \wedge K=n 
\wedge X=I \wedge Y=J \wedge Z=I-J+K) 
\rightarrow  d1(X,Y,Z) ).\label{cf:d1}\\
&\forall I J K 
X Y Z
((1\leq I\leq K\wedge 1\leq J\leq K \wedge K=n
\wedge X=I \wedge Y=J \wedge Z=I+J-1)
\rightarrow d2(X,Y,Z)
).\label{cf:d2}
\end{align}
Similarly, (L11) is not completable and was rewritten as (L25).
The set $\Gamma_C$, consisting of sentences (L25),  (L18), (L20),
~\eqref{cf:d1},~\eqref{cf:d2},~\eqref{f:countd1}, and~\eqref{f:countd2}, is completable.

We now note that we assume our user guide to denote (i) no predicate symbols as input and 
(ii) predicate symbol $q/2$ as the only output one.
Thus, predicate symbols $d1$ and $d2$ are private.
With that when we form the second-order completion for Program~C.
To begin with we from the first-order completion for Program~C. [Appendix B, AnthemP2P]
The first-order completion  is the conjunction of (L26), (L18), (L20), ~\eqref{f:countd1}, and~\eqref{f:countd2} and completed definitions for $d1$ and $d2$: 
\begin{align}
    &\forall 
X Y Z
(d1(X,Y,Z)
\leftrightarrow \exists I J K    
(1\leq I\leq K\wedge 1\leq J\leq K \wedge K=n 
\wedge X=I \wedge Y=J \wedge Z=I-J+K) 
   ).\label{compf:d1}\\
&\forall  
X Y Z
(d2(X,Y,Z) \leftrightarrow \exists I J K
    (1\leq I\leq K\wedge 1\leq J\leq K \wedge K=n
\wedge X=I \wedge Y=J \wedge Z=I+J-1)
).\label{compf:d2}
\end{align}
We now present a definition inspired by the concept of  
the second-order completion  defined  in [Appendix B, AnthemP2P].
Given program $P$, COMP($P$) is defined as the first-order completion
of formula representation of $P$, 
where all private symbols are replaced by predicate variables.
The extended completion $COMP^+(P)$ is defined as the conjunction of $COMP(P)$ with the conjunction of set definition formulas, where within that conjunction all function constants denoting sets ($s_i$) are replaced by function symbol variables and all predicate and function symbol variables are bound by existential quantifiers.


We utilize the following notation for a substitution: for a formula $F$, by $\sub{F}{t}{t'}$ we denote the formula constructed from $F$ by replacing all occurrences of term $t$ in $F$ with term $t'$. Similarly, for a formula $F$, by $\sub{F}{p}{p'}$ we denote the formula constructed from $F$ by replacing all occurrences of predicate symbol $p$ in $F$ with predicate symbol $p'$.
We allow ourself to abuse this notation and use a list composed of terms and predicate symbols.
For example,
recall that (L18) has the form
\[
\forall I \neg\bigl(
1 \leq I \leq n
\land
\neg\bigl(1 \leq \operatorname{count}(s2(I)) \leq 1\bigr)
\bigr)
\]
Thus, $\sub{(L18)}{s_2}{S_2}$ stands for
\[
\forall I \neg\bigl(
1 \leq I \leq n
\land
\neg\bigl(1 \leq \operatorname{count}(S_2(I)) \leq 1\bigr)
\bigr)
\]
Similarly, $\sub{\eqref{compf:d1}}{d1}{D_1}$ stands for
\[
\forall X Y Z
(D_1(X,Y,Z) \leftrightarrow \exists I J K
    (1\leq I\leq K\wedge 1\leq J\leq K \wedge K=n
\wedge X=I \wedge Y=J \wedge Z=I+J-1)
)
\]
Also, $\sub{\eqref{f:s6}}{s_6,d1}{S_6,D_1}$ stands for
\[
\big(\forall K V(V\in S_6(K) \leftrightarrow \exists I J( V=(I,J)\wedge q(I,J)\wedge D_1(I,J,K)))\big).
\]

The extended completion of Program C, namely $COMP^+(C)$ follows
\begin{align}   
&\exists D_1~D_2~ 
S_2~S_3~S_6~S_7\Big(
~  (L26) \wedge %\nonumber\\
%& 
 \sub{(L18)}{s_2}{S_2}
 \wedge 
%&
 \sub{(L20)}{s_3}{S_3} \wedge %\nonumber\\
 % & 
  \sub{\eqref{f:countd1}}{s_6}{S_6}\wedge %\nonumber\\
  %& 
  \sub{\eqref{f:countd2}}{s_7}{S_7}\wedge \nonumber\\
      &      \sub{\eqref{compf:d1}}{d1}{D1}
%      \forall 
%X Y Z
%(D1(X,Y,Z)
%\leftrightarrow \exists I J K    
%(1\leq I\leq K\wedge 1\leq J\leq K \wedge K=n 
%\wedge X=I \wedge Y=J \wedge Z=I-J+K) 
%)
\wedge\label{d1:eq}\\
&      \sub{\eqref{compf:d2}}{d2}{D2}
%\forall  
%X Y Z
%(D2(X,Y,Z) \leftrightarrow \exists I J K
%    (1\leq I\leq K\wedge 1\leq J\leq K \wedge K=n
%\wedge X=I \wedge Y=J \wedge Z=I+J-1)
%)
%
\wedge\label{d2:eq}\\
&\sub{(L17)}{s_2}{S_2}  \wedge \sub{(L19)}{s_3}{S_3}  \wedge\nonumber\\
& 
\sub{\eqref{f:s6}}{s_6,d_1}{S_6,D1}
\wedge\label{cd1:set}\\
&
\sub{\eqref{f:s7}}{s_7,d2}{S_7,D2}
 \Big)\label{cd2:set}
\end{align}

Let us recall that the firt-order completion of $\Gamma_B$ is the conjunction of  (L18), (L20), (L22), (L24), and (L26).
The extended completion of Program B, namely $COMP^+(B)$ follows
\begin{align}
    & \exists S_2~S_3~S_4~S_5 \big((L26) \wedge  %\nonumber\\
\sub{(L18)}{s_2}{S_2}
 \wedge 
%&
 \sub{(L20)}{s_3}{S_3} \wedge %\nonumber\\
\sub{(L22)}{s_4}{S_4} \wedge %\nonumber\\
\sub{(L24)}{s_5}{S_5} \wedge \nonumber\\
&\sub{(L17)}{s_2}{S_2}
 \wedge 
%&
 \sub{(L19)}{s_3}{S_3} \wedge \nonumber\\
&\sub{(L21)}{s_4}{S_4} \wedge %\nonumber\\
\sub{(L23)}{s_5}{S_5} \big) \nonumber
\end{align}



\section{Equivalence of Extended Completions of Programs B and C}

We now show how to derive the equivalence between the extended completion of program $B$ and the extended completion of program $C$, namely between $COMP^+(B)$ and $COMP^+(C)$ (listed in previous section), in classical logic from
\begin{itemize}
\item KB, a knowledge base describing integers, precomputed terms, sets and cardinalities
(see Section 6 [QVL]),
\item  UG, the pair of user guide asuumption formulas $\exists N(n = N)$ and $n \geq 1$, expressing that $n$ is a
placehodler for a positive integer.
\end{itemize}

We start by noting that  $COMP^+(B)$ is equivalent to the following formula:
\begin{align}
    & (L26) \wedge 
  \exists  
S_2~S_3\big( \sub{(L18)}{s_2}{S_2}
 \wedge 
%&
 \sub{(L20)}{s_3}{S_3} \wedge
 \sub{(L17)}{s_2}{S_2}  \wedge \sub{(L19)}{s_3}{S_3}\big)  \wedge\nonumber\\
 \exists S_4\Big(
  \sub{(L22)}{s_4}{S_4} \wedge
  \sub{(L21)}{s_4}{S_4} \Big)\wedge \nonumber\\
\exists S_4\Big(
  \sub{(L24)}{s_5}{S_5} 
  \sub{(L23)}{s_5}{S_5} \Big) 
  \wedge\nonumber\\
\end{align}


Check above and continue=============================


We then continue by noting that $COMP^+(C)$ is equivalent to the following formula:
\begin{align}
    & (L26) \wedge 
  \exists  
S_2~S_3\big( \sub{(L18)}{s_2}{S_2}
 \wedge 
%&
 \sub{(L20)}{s_3}{S_3} \wedge
 \sub{(L17)}{s_2}{S_2}  \wedge \sub{(L19)}{s_3}{S_3}\big)  \wedge\nonumber\\
&   \exists D_1~~S_6\Big(
  \sub{\eqref{f:countd1}}{s_6}{S_6}\wedge \nonumber\\ 
&\sub{\eqref{f:s6}}{s_6,d_1}{S_6,D1}
\wedge\label{cd1:set}\\
      &      \sub{\eqref{compf:d1}}{d1}{D1}
%      \forall 
%X Y Z
%(D1(X,Y,Z)
%\leftrightarrow \exists I J K    
%(1\leq I\leq K\wedge 1\leq J\leq K \wedge K=n 
%\wedge X=I \wedge Y=J \wedge Z=I-J+K) 
%)
\Big)
\wedge\label{d1:eq}\\
&  \exists
D_2~S_7\Big(
%& 
 %\nonumber\\
 % & 
  %& 
  \sub{\eqref{f:countd2}}{s_7}{S_7}\wedge \nonumber\\
&      \sub{\eqref{compf:d2}}{d2}{D2}
%\forall  
%X Y Z
%(D2(X,Y,Z) \leftrightarrow \exists I J K
%    (1\leq I\leq K\wedge 1\leq J\leq K \wedge K=n
%\wedge X=I \wedge Y=J \wedge Z=I+J-1)
%)
%
\wedge\label{d2:eq}\\
&
\sub{\eqref{f:s7}}{s_7,d2}{S_7,D2}
 \Big)\label{cd2:set}
\end{align}


Let us recall that the completion of $\Gamma_B$ is the conjunction of  (L18), (L20), (L22), (L24), and (L26).
In light of the composition of the completion of $\Gamma_B$ and the completion of $\Gamma_C$, it is sufficient to show the equivalence of the following formulas (in light of assumptions listed):
formula~\eqref{eq:complP2} and the conjunction of formulas (L21), (L22), (L23), and (L24).





We start by observing that 
conjunctive term~\eqref{d1:eq} is equivalent to
\begin{align}
&\forall   
X Y Z
(D1(X,Y,Z)
\leftrightarrow \exists I J     
(1\leq I\leq n\wedge 1\leq J\leq n
\wedge X=I \wedge Y=J \wedge Z=I-J+n) 
)\label{sd1:eq}
\end{align}
Similarly, conjunctive term~\eqref{d2:eq} is equivalent to
\begin{align}   
&\forall   
X Y Z
(D2(X,Y,Z)
\leftrightarrow \exists I J     
(1\leq I\leq n\wedge 1\leq J\leq n
\wedge X=I \wedge Y=J \wedge Z=I+J-1) 
)\label{sd2:eq}
\end{align}

In presence of~\eqref{d1:eq} (consider its simplified form~\eqref{sd1:eq}), conjunctive term~\eqref{cd1:set}
is equivalent to
\begin{align*}
    & \big(\forall K V(V\in s_6(K) \leftrightarrow \exists I J( V=(I,J)\wedge q(I,J)\wedge 
     \exists I_1 J_1     
(1\leq I_1\leq n\wedge 1\leq J_1\leq n
\wedge I=I_1 \wedge J=J_1 \wedge K=I_1-J_1+n)
\end{align*}    
which in turn can be simplified as
\begin{align*}
    & \forall K V(V\in s_6(K) \leftrightarrow \exists I J( V=(I,J)\wedge q(I,J)\wedge 
1\leq I\leq n\wedge 1\leq J\leq n
 \wedge K=I-J+n)) %\label{scd1:set}
\end{align*}    
In prsense of (L26), the last formula is equivalent
to
\begin{align}
    & \forall K V(V\in s_6(K) \leftrightarrow \exists I J( V=(I,J)\wedge q(I,J)
 \wedge K=I-J+n)) \label{simpscd1:set}
\end{align}    

In presence of~\eqref{d2:eq} (consider its simplified form~\eqref{sd2:eq}), conjunctive term~\eqref{cd1:set}
is equivalent to
\begin{align*}
    & \forall K V(V\in s_7(K) \leftrightarrow \exists I J( V=(I,J)\wedge q(I,J)\wedge 
1\leq I\leq n\wedge 1\leq J\leq n
 \wedge K=I+J-1)) %\label{scd2:set}
\end{align*}    
In prsense of (L26),the last formula is equivalent
to
\begin{align}
    & \forall K V(V\in s_7(K) \leftrightarrow \exists I J( V=(I,J)\wedge q(I,J)\wedge 
 K=I+J-1) \label{simpscd2:set}
\end{align}    




It is now easy to see that
the following conjunctive term of~\eqref{eq:complP2}
\begin{align}
&\exists D1 D2\Big(  
\eqref{d1:eq}\wedge\eqref{d2:eq}
\wedge\eqref{cd1:set}
\wedge\eqref{cd2:set}
\Big) \label{eq:existpart}
\end{align}
is equivalent to
 the conjunction of~\eqref{simpscd1:set} and~\eqref{simpscd2:set}.

We are now left to show that the conjunction of 
   \eqref{f:countd1}, \eqref{f:countd2},  \eqref{simpscd1:set}, and ~\eqref{simpscd2:set} 
is equivalent to the conjunction of
 (L21), (L22), (L23), and (L24) in our context.



 Observe how \eqref{f:countd1}, \eqref{f:countd2} can be simplified as follows:
 \begin{align}
        &\forall K \neg(   1\leq K\leq n\times 2-1\wedge \neg count(s_6(K))\leq 1).\label{sf:countd1}\\
        &\forall K \neg(   1\leq K\leq n\times 2-1\wedge \neg count(s_7(K))\leq 1).\label{sf:countd2}
\end{align}

Now, let us remind ourselves that formulas (L21), (L22), (L23), and (L24) are as follows:
\[
\forall K N V \,
\bigl(
V \in s_4(K,N)
\leftrightarrow
\exists I J \,
(V = (I,J) \land q(I,J) \land K = I - J + N)
\bigr),
\tag{L21}
\]
\[
\forall K N \,
\neg
\bigl(
1 \leq K \leq 2 \times N - 1
\land
\neg \operatorname{count}(s_4(K,N)) \leq 1
\land
N = n
\bigr),
\tag{L22}
\]
\[
\forall K V \,
\bigl(
V \in s_5(K)
\leftrightarrow
\exists I J \,
(V = (I,J) \land q(I,J) \land K = I + J - 1)
\bigr),
\tag{L23}
\]
\[
\forall K N \,
\neg
\bigl(
1 \leq K \leq 2 \times N - 1
\land
\neg \operatorname{count}(s_5(K)) \leq 1
\land
N = n
\bigr).
\tag{L24}
\]

We simplify  (L22) and (L24) as follows
 \begin{align}
        &\forall K \neg(   1\leq K\leq n\times 2-1\wedge \neg count(s_4(K,n))\leq 1).\label{f:sl22}\\
        &\forall K \neg(   1\leq K\leq n\times 2-1\wedge \neg count(s_5(K))\leq 1).\label{f:sl24}            
 \end{align}

 We now note that~\eqref{simpscd2:set} is identical to (L23) modulo name $s_7$ and $s_5$;
 similarly,~\eqref{sf:countd2} is identical to~\eqref{f:sl24}  modulo name $s_7$ and $s_5$.

We are now left to establish the exact relationship between~\eqref{sf:countd1}
and~\eqref{f:sl22}
 in light of relationship between~\eqref{simpscd1:set} and (L21). 
 Let us consider an instance of (L21) for a particular value of $N$, i.e, $N=n$:
 \begin{align*}
& \forall K V \,
\bigl(
V \in s_4(K,n)
\leftrightarrow
\exists I J \,
(V = (I,J) \land q(I,J) \land K = I - J + n)
\bigr)
 \end{align*}
This instance of (L21) is identical to~\eqref{simpscd1:set} modulo name $s_4(K,n)$ and $s_6(K)$.
Formulas~\eqref{sf:countd1}
and~\eqref{f:sl22} 
are also identical modulo names $s_6(K)$ and $s_4(K,n)$.

    \newpage
\section{Cryptarithmetic Problem Statement}
A common sample problem to illustrate the key concepts of constrain satisfaction paradigm is the cryptarithmetic problem. The problem is to find a mapping from letters to digits such that a given arithmetic equation holds true. For example, consider the following equation:
\begin{equation}
    \begin{array}{lr}
 &TWO\\
 +&TWO\\
 \hline
 &FOUR
\end{array}
\end{equation}
where we want to assign each letter a number from $0$ to $9$ (except we do not want $T$ or $F$ to be $0$ as customary in arithmetic to ``avoid'' leading zeros) so that they add as in arithmetic. 


    \section{Two Original ASP Programs}
    Here we provide two distinct solutions to the crypto arithmetic problem in ASP. The solutions were designed independently by two researchers in the field. The apriori agreement was to use binary predicate symbol {\tt asn/2} as the only output predicate that maps letters of the problem into numbers and no input predicates. 

    \paragraph{Yuliya's Solution}
    \lstinputlisting[language=Prolog, numbers=left, caption="Yuliya's ASP encoding for two+two=four"]
{cryptoYuliya.lp}

    \paragraph{Vladimir's Solution}
    \lstinputlisting[language=Prolog, numbers=left, caption="Vladimir's ASP encoding for two+two=four"]
{cryptoVladimir.lp}

It is easy to see that solutions differ. Answer set solver {\sc clingo} takes orders of magnitude longer to find solutions to this puzzle using Vladimir's encoding.
 %To begin with they use different signatures to solve the problem. 
Yet, we would like to reason whether these programs are essentially equivalent (semantically) without having to run these and compare their answer sets. We follow approach of {\em Reasoning about Logic Programming Solutions
to the N-Queens Puzzle} by Vladimir Lifschitz to perform this task.

\section{Program's rewritten to bypass common abbreviations}

    \paragraph{Yuliya's Solution in ``Canonical form''}

    Lines 1, 3, and 7 in the original encoding by Yuliya are abbreviations for the following code.
     \lstinputlisting[language=Prolog, caption="Yuliya's Part ASP encoding with abbreviations removed"]
{cryptoYuliyaCannonicalPart.lp}

    \paragraph{Vladimir's Solution in ``Canonical form''}
        Lines 1 and 3 in the original encoding by Vladimir are abbreviations for the following code.

 \lstinputlisting[language=Prolog, caption="Vladimir's Part ASP encoding with abbreviations removed"]
{cryptoVladimirCannonicalPart.lp}

\section{Furmula representations    of two solutions}
\paragraph{Yuliya's Solution in Furmula representation}

\begin{align}
&named(\alpha). & \alpha\in \{t,w,o,f,o,u,r\} \\
\end{align}
\end{document}    